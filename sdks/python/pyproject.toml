[build-system]
requires = ["setuptools==84.0.0"]
build-backend = "setuptools.build_meta"
[project]
name = "weaveport-sdk"
version = "0.1.0"
description = "Local WeavePort proof-of-concept author SDK"
requires-python = ">=3.11"
