## Approach
Inventory every maintained Markdown/text document and its ownership. Read current guides and examples against public API baselines, package matrices, scripts and source. Classify dated archives/reports/licenses/fixture content separately so historical facts are not rewritten as current claims. Explain differences between released 0.7.0 artifacts and qualified post-release source additions without allocating a new version.

Keep canonical prose in maintained guides; regenerate AI references and build the complete website. Include optional APIs, failure codes and contributor conventions in retrieval and navigation. Extend existing documentation checks for discovered coverage gaps rather than testing prose mechanically.

## Verification
Strict OpenSpec validation, generated-reference checks, public-tree and documentation regressions, complete DocFX build with link/anchor/search/retrieval checks, and relevant example/package checks where instructions change. Reuse the preceding native package qualification for unchanged runtime/example code; do not infer new platform qualification from documentation edits.

## Specification wording
Clarify existing scope without changing behavior: packaged execution is no longer a PoC-only description; package verification also covers public releases; sequential registration ordering applies to exclusive registrations; the read-only endpoint-mount scenario applies to Docker, as already stated by tenant-isolation's purpose. No new requirement, capability or runtime policy is introduced.

## External references checked on 2026-09-24
- [llms.txt proposal](https://llmstxt.org/): compact linked index, Markdown alternatives and discovery links; the full reference remains an optional companion.
- [GitHub MCP remote configuration](https://github.com/github/github-mcp-server/blob/main/docs/remote-server.md): repository toolset and read-only path remain documented. This is not an authenticated client test.
- [DocFX Markdown guidance](https://dotnet.github.io/docfx/docs/markdown.html): canonical Markdown input and rendered link handling; verified with the pinned local DocFX build.
