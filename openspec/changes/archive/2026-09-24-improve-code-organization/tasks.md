## 1. Dependencies and C# ownership

- [x] 1.1 Centralize maintained NuGet versions, preserve standalone templates, restore and build.
- [x] 1.2 Group C# files by responsibility, separate contract types and update linked source paths; pass code style and host regressions.

## 2. Multilingual SDK structure

- [x] 2.1 Extract TypeScript registration/runtime modules and cohesive stream handling; expand control flow and compile.
- [x] 2.2 Extract Python registration/runtime/context modules, clarify callback flow and preserve import compatibility.
- [x] 2.3 Run cross-language protocol, stream and package-consumer regressions after the extraction.

## 3. Diagnostics and prevention

- [x] 3.1 Name and document existing generated event IDs; add stable platform exception codes and regression assertions.
- [x] 3.2 Document source navigation and extension rules; add structural verification to maintained check entry points.

## 4. Verification

- [x] 4.1 Run fresh packed consumers and applicable host, reuse, scheduler and SDK checks; record evidence and limitations.
- [x] 4.2 Validate specifications, public tree, documentation links, generated docs and diff; synchronize verified diagnostic requirements.
