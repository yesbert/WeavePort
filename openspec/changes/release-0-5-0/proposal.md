# Release 0.5.0

## Why
Publish the fully integrated seven-package delivery after an additional audit.

## What Changes
Harden malformed gateway batch validation, qualify the final source and packages, align release guidance, and publish the original tested artifacts. Existing runtime requirements and API/protocol contracts are preserved.

## Impact
Gateway client validation, packed regression fixtures, release documentation and publication evidence. No tag is moved and no published 0.4.0 package is replaced.
