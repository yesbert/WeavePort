[build-system]
requires = ["setuptools==84.0.0"]
build-backend = "setuptools.build_meta"
[project]
name = "weaveport-sdk"
version = "0.2.0"
description = "WeavePort author SDK with registered session cleanup"
requires-python = ">=3.11"
