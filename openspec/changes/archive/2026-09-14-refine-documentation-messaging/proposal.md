## Why

The initial website explains execution boundaries before showing why a developer would choose WeavePort. The README is still a repository inventory. Both need a clear audience, concrete use cases and a direct path to a working example.

## What Changes

- Rewrite the README and website entry pages around extending a .NET product through C#, Python and TypeScript plugins.
- Show a small, source-grounded language example, concrete integration benefits and runnable application examples.
- Refine typography, spacing, navigation, code presentation, mobile layouts and dark mode, informed by Blume and other open-source documentation sites.
- Review the published content surface for clear entry points and retained support boundaries.

## Capabilities

Documentation and presentation only (`skip_specs: true`); existing product contracts are unchanged.

## Impact

README, website content and theme, canonical guide introductions, site generation/navigation and validation. Regenerate AI retrieval files and prepare a replacement staging artifact. Public activation and package publication remain separate.
